const fs = require('fs');
// Let's create a small script that fetches categories from the backend directly if possible. Wait, can we? 
// No, but we can see the console.logs from the react-native app if we had it.
// I can just check if any keyword matching is buggy.
